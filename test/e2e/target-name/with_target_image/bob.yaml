dependencies: [ docker ]
build:
  build:
    cmd: docker build -t my-image:latest .
    target:
      image: my-image:latest
  another:
    cmd: docker build -t my-image:latest .
    target:
      image: my-image:latest